.. This is a placeholder page so that it shows up on the left sidebar,
   but gets redirected to userguide/index.html. This is because the
   index page has a lot of good introductory information but may get lost
   since index pages aren't included in the sidebar (it's only accessible from
   the navbar "User Guide" link).

.. It requires a title so that it can be included in the sidebar, but
   this is never seen.

Introduction
============
